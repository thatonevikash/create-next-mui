---
title: Default Configs
description: Default configuration files
---

Default configurations and options

## Default template

- TypeScript
- No features

## Default files

- `eslint`
  - `perfectionist` plugin beautifully organizes the import statements.
  - `unused-imports` plugin gracefully removes the unused import statements.
- `.vscode`
  - `settings.json` to apply project specific snippets
