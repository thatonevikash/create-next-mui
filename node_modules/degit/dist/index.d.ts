import EventEmitter from "node:events";

//#region src/repo.d.ts
type Repo = {
  mode: 'tar' | 'git';
  name: string;
  ref: string;
  site: string;
  ssh: string;
  subdir?: string;
  url: string;
  user: string;
};
//#endregion
//#region src/index.d.ts
type ExecResult = {
  stderr: string;
  stdout: string;
};
type ExecFn = (command: string, args?: string[]) => Promise<ExecResult>;
type FetchFn = (url: string, dest: string, proxy?: string) => Promise<void>;
type ConstructorOptions = {
  cache?: boolean;
  exec?: ExecFn;
  fetch?: FetchFn;
  force?: boolean;
  mode?: 'tar' | 'git';
  verbose?: boolean;
};
type EventInfo = {
  code?: InfoCode | DegitErrorCode;
  dest?: string;
  message: string;
  repo?: Repo;
  url?: string;
  original?: unknown;
  ref?: string;
};
type Directive = {
  action: 'clone';
  cache?: boolean;
  src: string;
  verbose?: boolean;
} | {
  action: 'remove';
  files: string | string[];
};
type CloneDirective = Extract<Directive, {
  action: 'clone';
}>;
type RemoveDirective = Extract<Directive, {
  action: 'remove';
}>;
type DirectiveActions = {
  clone: (dir: string, dest: string, action: CloneDirective) => Promise<void>;
  remove: (dest: string, action: RemoveDirective) => void;
};
type Options = ConstructorOptions;
type ValidModes = 'tar' | 'git';
type InfoCode = 'SUCCESS' | 'FILE_DOES_NOT_EXIST' | 'REMOVED' | 'DEST_NOT_EMPTY' | 'DEST_IS_EMPTY' | 'USING_CACHE' | 'FOUND_MATCH' | 'FILE_EXISTS' | 'PROXY' | 'DOWNLOADING' | 'EXTRACTING';
type DegitErrorCode = 'DEST_NOT_EMPTY' | 'MISSING_REF' | 'COULD_NOT_DOWNLOAD' | 'BAD_SRC' | 'UNSUPPORTED_HOST' | 'BAD_REF' | 'COULD_NOT_FETCH';
type Info = EventInfo;
type Action = Directive;
type DegitAction = CloneDirective;
type RemoveAction = RemoveDirective;
declare function degit(src: string, opts?: ConstructorOptions): Degit;
declare class Degit extends EventEmitter {
  cache?: boolean;
  force?: boolean;
  verbose?: boolean;
  proxy?: string;
  repo: Repo;
  mode: 'tar' | 'git';
  _fetch: FetchFn;
  _exec: ExecFn;
  _hasStashed: boolean;
  directiveActions: DirectiveActions;
  constructor(src: string, opts?: ConstructorOptions);
  _getDirectives(dest: string): Directive[] | false;
  clone(dest: string): Promise<void>;
  remove(dest: string, action: RemoveDirective): void;
  _checkDirIsEmpty(dir: string): void;
  _info(info: EventInfo): void;
  _warn(info: EventInfo): void;
  _verbose(info: EventInfo): void;
  _getHash(repo: Repo, cached: Record<string, string>): Promise<string | undefined>;
  _getHashFromCache(repo: Repo, cached: Record<string, string>): string | undefined;
  _selectRef(refs: Array<{
    hash: string;
    name?: string;
    type?: string;
  }>, selector: string): string | null | undefined;
  _cloneWithTar(dir: string, dest: string): Promise<void>;
  _cloneWithGit(dir: string, dest: string): Promise<void>;
  _fetchRefs(repo: Repo): Promise<({
    hash: string;
    type: string;
    name?: undefined;
  } | {
    hash: string;
    name: string;
    type: string;
  })[]>;
  _untarWithRetry(file: string, dest: string, subdir: string | null, url: string): Promise<void>;
}
//#endregion
export { Action, DegitAction, DegitErrorCode, Info, InfoCode, Options, RemoveAction, ValidModes, degit as default };
//# sourceMappingURL=index.d.ts.map